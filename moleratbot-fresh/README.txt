# Moleratbot Music Fix

Replace these two files in your project:

- package.json
- discord_bot.js

Then push/redeploy on Railway. The deploy log should say:

✅ Music dependencies loaded

Use /join first, then use !play or /play in the music request text channel.
